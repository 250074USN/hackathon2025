import { Routes, Route } from "react-router-dom";


function Routing( ) {
    return (
        <Routes>
            <Route path="/" element={<Ind ={/>} />
        </Routes>
    );
}

export default Routing;
